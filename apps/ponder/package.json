{
  "name": "@morpho-blue-liquidation-bot/ponder",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ponder dev",
    "start": "ponder start",
    "db": "ponder db",
    "codegen": "ponder codegen",
    "lint": "eslint . --fix",
    "typecheck": "tsc",
    "dev:test": "timeout 20m ponder start --schema test/ponder.schema.ts --config test/ponder.config.ts",
    "test": "concurrently \"pnpm run dev:test\" \"pnpm vitest indexer\""
  },
  "dependencies": {
    "hono": "^4.5.0"
  },
  "devDependencies": {
    "@ponder/client": "^0.9.27",
    "concurrently": "^9.1.2",
    "vitest": "^3.0.9"
  }
}
